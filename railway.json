{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pip install -r mainProject/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd mainProject && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn mainProject.wsgi:application --bind 0.0.0.0:$PORT --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}